# Optional: NewsAPI key for live sentiment headlines (https://newsapi.org)
# If unset, sentiment uses deterministic demo fallback — clearly labeled in the UI.
NEWS_API_KEY=

# Optional: Yahoo Finance works without a key via yfinance.
# No market-data API key is required; the app falls back to cached/demo data automatically.
